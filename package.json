{
  "name": "@vectojs/brings-mcp",
  "version": "0.1.0",
  "description": "Local-first MCP server for Brings design documents.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "brings-mcp": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "bun test",
    "format:check": "prettier --check .",
    "lint": "oxlint --deny-warnings src test",
    "verify": "bun run format:check && bun run lint && bun run test && bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "oxlint": "1.73.0",
    "prettier": "3.9.5",
    "typescript": "7.0.2"
  }
}
